{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.1.3",
    "@types/node": "^20.3.1",
    "lerna": "^7.0.2",
    "luxon": "^3.3.0",
    "mongodb": "^5.6.0"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "build": "lerna run tsc",
    "dev:run-api":"cd ./packages/api && npm run start"

  }
}